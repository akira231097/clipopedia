# Core runtime + offline demo (matches [project.dependencies] in pyproject.toml).
# For the live backend adapters, also install: pip install -e ".[live]"
pydantic>=2.6
pydantic-settings>=2.2
numpy>=1.26
rapidfuzz>=3.6
python-dotenv>=1.0
tenacity>=8.2
rich>=13.7
