# Adapters for the "live" backend (real external services).
# Install on top of requirements.txt when you want to run against real APIs.
openai>=1.30
cohere>=5.5
pinecone>=5.0
pinecone-text>=0.9
langgraph>=0.2
tweepy>=4.14
boto3>=1.34
psycopg[binary]>=3.1
